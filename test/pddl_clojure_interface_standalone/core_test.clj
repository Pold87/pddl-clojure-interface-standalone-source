(ns pddl-clojure-interface-standalone.core-test
  (:require [clojure.test :refer :all]
            [pddl-clojure-interface-standalone.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
